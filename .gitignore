__pycache__/
*.pyc
.venv/
node_modules/
dist/
*.tsbuildinfo
.DS_Store

# Durable relay SQLite artifacts
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm

# Local-only research, review, and historical artifacts.
# Tracked nothing here; the distributed tree is the frozen v1 package.
local/

# spec/ holds ONLY the two frozen release files.
# Block any other file (planning docs, briefs, etc.) from being accidentally added.
spec/*
!spec/SPP-v1-Core-Protocol-Specification.md
!spec/SPP-v1-External-Review-Guide.md
